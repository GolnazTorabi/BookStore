<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/order_graph"
    app:startDestination="@id/orderFragment">

    <fragment
        android:id="@+id/orderFragment"
        android:name="com.book.store.stock.bookstore.pages.order.OrderFragment"
        android:label="order_fragment"
        tools:layout="@layout/order_fragment" >
        <action
            android:id="@+id/action_orderFragment_to_orderScannerFragment"
            app:destination="@id/orderScannerFragment" />
    </fragment>
    <fragment
        android:id="@+id/orderScannerFragment"
        android:name="com.book.store.stock.bookstore.pages.order.scanner.OrderScannerFragment"
        android:label="fragment_order_scanner"
        tools:layout="@layout/fragment_order_scanner" />
</navigation>